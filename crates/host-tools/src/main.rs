//! CLI entry (stub): use `cargo run -p xtask` for firmware build orchestration.

fn main() {
    eprintln!("galdr-host: stub — integrate flashing SDK and manifest signing.");
    std::process::exit(1);
}

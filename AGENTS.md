# Agent instructions

Coding agents (**Cursor**, **Claude Code**, and similar) should follow:

- **[CLAUDE.md](CLAUDE.md)** — project-wide behaviour, git etiquette, doc pointers.
- **[`.cursor/rules/`](.cursor/rules/)** — Cursor rules (`.mdc`): always-on project defaults, Rust patterns, `xtask` / `TEST_RESULTS.md` sync.
